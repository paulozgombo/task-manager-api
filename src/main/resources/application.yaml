spring:
  application:
    name: task-manager-api
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:taskdb
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
